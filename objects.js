
const bouncer = (age) => {
    if (age >= 21) {
        console.log('DRINK!!');
    } else {
        console.log('SHAME!');
    };
}

bouncer(12);
bouncer(93);

const me = 'Martin';
console.log(me.length);